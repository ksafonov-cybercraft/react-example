import { Meta, Story, Canvas } from "@storybook/addon-docs";

import TimePicker from "./TimePicker";

<Meta title="MDX/Feedback/ActionPanel" component={TimePicker} />

# TimePicker

In this doc, you can see how to implement `ActionPanel` component
to Modularity app with needed component state. For it, you should
click on `Show code` in the lower right corner.
Happy using this component!

`ActionPanel` component with `primary` button in default position

<Canvas>
    <Story name="TimePicker" height="200px">
        <TimePicker />
    </Story>
</Canvas>